# 开发路由

PUA 是 Electron + React / Pi 薄壳。真实安全约束、会话隔离与异步行为优先于界面演示；修改时保留调用方已有能力与用户改动。

进行架构重构、新增跨模块功能、调整进程边界、IPC、状态所有权或目录结构前，必须完整阅读 [PUA 目标架构](docs/target-architecture.md)，并用当前代码、测试和 Git 状态校正文档漂移。该文档描述目标，不代表重构已经执行。

修改 UI、主题、动效或工作台模块前，必读 [组件库原则与流程](src/renderer/ui/README.md) 和 [模块接入契约](src/renderer/modules/README.md)。封装判断、预览验证和生产接入顺序以这两份文档为准。

- 跨场景复用或具有统一交互、可访问性、动效状态的控件进入 `ui`；生产工作台领域组合进入对应 `renderer/features/*`。`renderer/modules` 仅作为现有预览迁移来源，不再新增生产聚合。一次性业务分支留调用方，避免纯透传和万能组件。
- 先查库 → 复用或扩展 → 新增必要接口 → 预览与测试 → 用户确认后生产替换。
- 预览只使用隔离的本地数据，不能调用真实 Pi、Git 或磁盘；生产入口不能导入 fixture。处于用户确认阶段时，不自动接入 App。

以上是开发路由，不扩大任务授权，也不改变宿主、安全或工具约束。

阶段任务完成后，直接提交本阶段改动并推送当前分支，再开始下一阶段；不夹带无关改动。推送失败时停止并报告。提交不扩大验证授权，例如用户要求不运行自动测试时，不得为了提交额外运行测试。
